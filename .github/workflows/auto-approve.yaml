name: auto-approve-assign
on: pull_request

jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - uses: hkusu/review-assign-action@v1.3.1
        with:
          reviewers: dlevenson44, dlevenson44bot
          max-num-of-reviewers: 2

