# FE Monorepo
test
This is a personal monorepo project used to host NextJS applications, shared UI components, and shared UI configuration.

# Requirements
You must be running `NodeJS` 20.9.0 or newer, and you should have `pnpm` 8.10.2 or newer.

# Commands